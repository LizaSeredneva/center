#include <stdio.h> //Подключение библиотеки ввода/вывода
#include <math.h> //Математические функции
int main()
{
    int a=3;
    int b=4; //Объявление переменной
    int n; //Объявление переменной, где n-среднее арифметическое
    n = (a + b)/2; //Арифметические операции
        printf("n: %d",n); //Вывод
    return 0;
}